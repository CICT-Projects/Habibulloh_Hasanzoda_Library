# 📚 Библиотека - CRUD приложение

## ✅ Что работает:

### Backend (http://localhost:5000)
- **GET /api/books** - получить все книги
- **GET /api/books/{id}** - получить книгу по ID
- **POST /api/books** - создать новую книгу
- **PUT /api/books/{id}** - обновить книгу
- **DELETE /api/books/{id}** - удалить книгу

- **GET /api/authors** - получить всех авторов
- **GET /api/authors/{id}** - получить автора по ID
- **POST /api/authors** - создать нового автора
- **PUT /api/authors/{id}** - обновить автора
- **DELETE /api/authors/{id}** - удалить автора

### Frontend (http://localhost:5174)
- Вкладка "Книги" - управление книгами
- Вкладка "Авторы" - управление авторами

## 🔧 Начальные данные в БД:

### Авторы:
- Лев Толстой (Россия)
- Федор Достоевский (Россия)
- Иван Тургенев (Россия)
- Антон Чехов (Россия)

### Книги:
- Война и мир (1869)
- Преступление и наказание (1866)
- Дворянское гнездо (1859)
- Вишневый сад (1904)
- Анна Каренина (1877)

## 📝 Функции в UI:

1. **Просмотр** - Все элементы отображаются в списке при загрузке
2. **Создание** - Форма в верхней части страницы, кнопка "Добавить"
3. **Редактирование** - Кнопка ✏️ рядом с каждым элементом
4. **Удаление** - Кнопка ✕ рядом с каждым элементом

## 🚀 Как запустить:

### Backend:
```powershell
cd D:\Habibulloh_Hasanzoda_Library\backend
dotnet run
```

### Frontend:
```powershell
cd D:\Habibulloh_Hasanzoda_Library\frontend
npm run dev
```

## 🧪 Тестирование API через curl:

### Получить все книги:
```powershell
curl -X GET http://localhost:5000/api/books
```

### Создать новую книгу:
```powershell
curl -X POST http://localhost:5000/api/books `
  -H "Content-Type: application/json" `
  -d '{"title":"Новая книга","year":2025}'
```

### Обновить книгу (ID=1):
```powershell
curl -X PUT http://localhost:5000/api/books/1 `
  -H "Content-Type: application/json" `
  -d '{"id":1,"title":"Обновленная название","year":2025}'
```

### Удалить книгу (ID=1):
```powershell
curl -X DELETE http://localhost:5000/api/books/1
```
